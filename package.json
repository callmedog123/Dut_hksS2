{
  "name": "the-unclicked-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "node scripts/check-syntax.js",
    "test": "node --test",
    "test:messages": "node --test tests/messages.test.js",
    "build": "node scripts/validate-build.js && node scripts/validate-release.js"
  }
}
